# End Sem

1. Write a menu driven TCP client-server program, where the client can select from the options :
    - send a msg
    - request current date from the server
    - send the number and receive its factorial
    - exit the connection

2. Design a TCP client program where the client sends a menu of operations to server :
    - addition, sub, mul, divide, exit

3. Byte stuffing and bit stuffing program

4. packet tracer for :
    - mesh topology
    - star topology